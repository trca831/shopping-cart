<h1>Cart Item Shown Here:</h1>
<ul>
  <% @cart.cart_items.each do |cart_item| %>
    <li>
      <%= cart_item.product.name %>
      Quantity: <%= cart_item.quantity %>
      <!-- You can add additional information about the product here -->
    </li>
  <% end %>
</ul>

<h1> <%= @cart.name %> </h1> 

<p> It costs $<%= @cart.price%> per item. </p>
<p> Currently buying <%= @cart.amount%> <%= @cart.name%>.  </p>

<%=  link_to "New Cart Item", new_cart_cart_item_path(@cart), class: "btn btn-primary" %>